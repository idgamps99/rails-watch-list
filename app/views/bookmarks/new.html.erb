<%= render :partial => "shared/navbar" %>
<div class="container">
 <%= simple_form_for [@list, @bookmark] do |f| %> <%# there's a bug here when you try to add a non-unique pair %>
    <%= f.label :comment %>
    <%= f.text_field :comment %>
    <%= f.association :movie %>
    <%= f.submit "Add to List", class: "btn btn-primary" %>
  <% end %>
</div>
